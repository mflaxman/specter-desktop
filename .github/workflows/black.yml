name: Blackify Action
on: push

jobs:
  build:
    name: Blackify
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master
    - name: Black Code Formatter
    - uses: lgeiger/black-action@v1.0.1
    - run: black --check .
